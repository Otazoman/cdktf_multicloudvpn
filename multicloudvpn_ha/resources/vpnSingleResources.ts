/* AWS */
// 仮想プライベートゲートウェイの設定(そのまま使える)
// 仮想プライベートゲートウェイのルート伝播の設定(そのまま使える)
// カスタマーゲートウェイの設定(そのまま使えそう)
// サイト間のVPN接続の設定(そのまま使えそう)
// Azure側へのルート設定(独自作成必要)

/* Azure */
// GatewaySubnet作成(そのまま使えそう)
// PublicIP取得(そのまま使えそう)
// VPNGateway作成(シングルパターンの修正必要)
// localGatewayとトンネル作成(シングルパターンの修正必要)

/* Google */
// 外部IPアドレスの設定(独自必要かも)
// パケット転送ルールの設定(独自必要)
// VPNトンネルの設定(独自必要かも)
